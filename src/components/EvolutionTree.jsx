import React from 'react'

export default function EvolutionTree({ chain, currentId, onSelect }) {
  // Componente temporal vacío para evitar el error
  return (
    <div style={{ 
      fontSize: '10px', 
      color: '#0f380f', 
      textAlign: 'center',
      padding: '8px',
      background: 'rgba(0,0,0,0.1)',
      borderRadius: '4px'
    }}>
      Evolution Tree (temporal)
    </div>
  )
}