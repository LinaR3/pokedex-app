import { useEffect } from 'react'

/**
 * Hook vacío temporal para evitar el error de importación.
 * Puedes implementarlo después si quieres soporte táctil.
 */
export function useSwipe(ref, handlers) {
  // Por ahora no hace nada (solo evita que la app se rompa)
  useEffect(() => {
    // Swipe desactivado temporalmente
  }, [])
}